<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/itemMarH"
    android:layout_marginVertical="@dimen/itemMarV"
    android:background="@drawable/yellow_box"
    android:paddingHorizontal="@dimen/itemPadH"
    android:paddingVertical="@dimen/itemPadV">

    <ir.mahdiparastesh.sexbook.more.LiveEditText
        android:id="@+id/name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10sp"
        android:autofillHints="addressRegion"
        android:background="@null"
        android:fontFamily="@font/bold"
        android:hint="@string/plItemHint"
        android:inputType="textMultiLine"
        android:lineSpacingMultiplier="1.3"
        android:maxLines="5"
        android:textIsSelectable="true"
        android:textSize="@dimen/nameFont"
        app:layout_constraintEnd_toStartOf="@id/sum"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/sum"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/bold"
        android:textSize="@dimen/sumFont"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>