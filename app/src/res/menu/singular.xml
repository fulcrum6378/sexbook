<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/identify"
        android:icon="@drawable/person"
        android:showAsAction="ifRoom"
        android:title="@string/identify"
        android:tooltipText="@string/identify" />

    <item
        android:id="@+id/otherPartners"
        android:icon="@drawable/people"
        android:iconTint="?textColorPrimaryContainer"
        android:showAsAction="ifRoom"
        android:title="@string/otherPartners"
        android:tooltipText="@string/otherPartnersDesc"
        android:visible="false" />
</menu>